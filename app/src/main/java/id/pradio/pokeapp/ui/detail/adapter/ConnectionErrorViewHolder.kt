package id.pradio.pokeapp.ui.detail.adapter

import id.pradio.pokeapp.core.BaseViewHolder
import id.pradio.pokeapp.databinding.ListItemConnectionErrorBinding
import id.pradio.pokeapp.ui.detail.ConnectionError

class ConnectionErrorViewHolder(
    private val binding : ListItemConnectionErrorBinding
) : BaseViewHolder<ConnectionError>(binding.root){
    override fun bind(model: ConnectionError) {

    }
}